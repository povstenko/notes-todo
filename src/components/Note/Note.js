import React from 'react';
import PropTypes from 'prop-types';
import './Note.css';

const Note = () => (
  <div className="Note">
    Note Component
  </div>
);

Note.propTypes = {};

Note.defaultProps = {};

export default Note;
